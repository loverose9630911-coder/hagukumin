# ぞうさんとなかまたち（PHP + Python 版）

生成AIで制作したオリジナル動物キャラクター **6匹（ぞう・うさぎ・カピバラ・キリン・パンダ・ビーバー）**
でつなげて消す、ツムツム系のパズルゲームです。
アプリアイコンは **ぞうさん** を PHP-GD で描き起こしています。

- **ゲームのルールは Python**（物理・チェーン判定・スコア・スキル）
- **Web アプリは PHP**（画面・セーブ・ショップ・ランキング・画像生成）
- ブラウザ側の JavaScript は **描画と指の動きを送るだけ**

iPad へは **Netlify に置いてホーム画面に追加**するだけで入ります → [iPad に入れる（Netlify）](#ipad-に入れるnetlify)。

画像素材・音声素材・外部ライブラリは一切使っていません。キャラクターの絵は
PHP-GD が図形を組み合わせてその場で描き出し、効果音と BGM は WebAudio で合成しています。

---

## うごかしかた

```bash
cd tsumtsum
bin/serve                 # → http://localhost:8080
```

`bin/serve` が Python のエンジン（ポート 8765）と PHP の Web アプリ（ポート 8080）を
両方立ち上げます。必要なら初回に config とキャラ画像も自動で作ります。

```bash
make setup   # キャラ定義の書き出し＋画像の生成だけ
make static  # iPad 用の静的サイト（dist/）を書き出す
make test    # エンジンのテスト（42 件）
make sim     # ブラウザ無しで自動プレイしてバランスを確認
make assets  # キャラ画像とアプリアイコンを描きなおす
```

必要なもの: PHP 8.1 以上（`gd` / `pdo_sqlite` / `curl`）、Python 3.9 以上（標準ライブラリのみ）。

---

## なぜこの分担なのか

ブラウザで動くゲームである以上、60fps の描画と指の追従だけは JavaScript が担当するしかありません。
そこで「**ルールと判定は 1 か所（Python）だけに置く**」という形にしました。

```
  ブラウザ                    PHP                        Python エンジン
 ┌──────────┐        ┌──────────────┐        ┌────────────────────┐
 │ なぞった   │  ids   │ セッション管理  │  ids   │ チェーン判定        │
 │ ツムの ID  │ ─────> │ プレイヤー保存  │ ─────> │ 消去・スコア・物理   │
 │           │        │ ランキング     │        │ 盤面を静止させる     │
 │ 静止盤面を │ <───── │              │ <───── │ 静止盤面 + スコア    │
 │ 補間して描く│  盤面   │              │  盤面   │                    │
 └──────────┘        └──────────────┘        └────────────────────┘
```

- ブラウザが送れるのは「どのツムをなぞったか」だけ。**スコアは送れません。**
- 消去・得点・時間はすべてサーバー側で決まるので、ブラウザを書きかえても点数を偽装できません。
- 盤面は 1 手ごとに **完全に静止した状態** で返ってきます。ブラウザはその間を補間して
  「落ちてくる」ように見せるだけなので、物理エンジンをクライアントに持つ必要がありません
  （ルールの二重実装がない）。

1 手あたりのエンジンの計算は実測 **平均 39ms / 最大 75ms**。
落下アニメーション（0.42 秒）のあいだに終わるので、待たされる感じはありません。

---

## iPad に入れる（Netlify）

いつもの **Netlify に置いて、ホーム画面に追加** で遊べます。
サーバー（PHP / Python）は要りません。**一度ひらけば機内モードでも動きます。**

### 1. 公開する

公開用のフォルダは **`tsumtsum/dist/`** です。リポジトリに入れてあるので、
そのまま使えます（中身を変えたときだけ `make static` で作りなおしてください）。

**A. ドラッグ＆ドロップ（いちばん手軽）**

1. <https://app.netlify.com/drop> を開く
2. `tsumtsum/dist` フォルダを**まるごと**ドラッグ＆ドロップ
3. `https://〇〇.netlify.app` が発行される
   （`Site settings → Change site name` で好きな名前にできます）

**B. GitHub とつなぐ（更新が自動になる）**

1. Netlify で `Add new site → Import an existing project` からこのリポジトリを選ぶ
2. 設定はさわらなくて大丈夫です
   （リポジトリ直下の `netlify.toml` で公開フォルダを `tsumtsum/dist` に指定済み。ビルドコマンドは無し）
3. 以降は `make static` して push すれば自動で反映されます

### 2. iPad のホーム画面に追加する

1. **Safari** でその URL を開く（Chrome だとホーム画面に追加できません）
2. **共有ボタン** <kbd>↑</kbd> をタップ
3. **「ホーム画面に追加」** → そのまま「追加」

ぞうさんのアイコンがホーム画面に並びます。
アイコンから起動すると Safari のバーが出ない全画面になり、
**あとはオフラインでも遊べます**（Service Worker が中身を持っておきます）。

> 保存されるもの（コイン・所持キャラ・レベル・ハイスコア・ランキング）は、
> その iPad の中だけです。別の端末とは共有されません。

### 中身を変えたとき

```bash
cd tsumtsum
make static     # dist/ を作りなおす
```

そのあと Netlify にドロップしなおすか、コミットして push すれば反映されます。
iPad 側は、アプリを閉じて開きなおすと新しいものに入れかわります。

---

## サーバー版（PHP + Python）を動かす場合

ランキングをみんなで共有したい、スコアを改ざんできないようにしたい、というときは
サーバー版を動かします。iPad からも同じように遊べます。

```bash
cd tsumtsum
bin/serve
```

同じ Wi-Fi 用のアドレス（`http://192.168.x.x:8080`）が表示されるので、
それを iPad の Safari で開いて「ホーム画面に追加」すれば遊べます。
`Dockerfile` と `render.yaml`（Render の Blueprint）も同梱してあるので、
クラウドに置いていつでも遊べるようにもできます。

### 2 つの版のちがい

| | Netlify 版（おすすめ） | サーバー版 |
| --- | --- | --- |
| 置き場所 | Netlify | PHP と Python が動くところ |
| オフライン | ○ | × |
| ゲームのルール | ブラウザ内エンジン（`assets/js/engine-local.js`） | Python エンジン |
| セーブ | その端末の中だけ | SQLite（みんなで共有） |
| ランキング | 自分のきろく 20 件 | 全員のランキング |
| スコアの改ざん | できてしまう（手元で完結するため） | できない（サーバーが計算） |

数値（重力・スコア式・フィーバー条件…）は両方とも `config.json` を読むので、
`engine/zousan/` の Python を直せば両方に反映されます。

---

## あそびかた

| ルール | 内容 |
| --- | --- |
| つなぐ | おなじキャラのツムを **3こ以上** なぞってはなすと消える（となり同士のみ） |
| コンボ | つづけて消すと倍率が上がる（最大 2 倍）。2.2 秒とぎれるとリセット |
| ボム | **7こ以上**つなぐと出現。タップで周囲をまとめて消す |
| タイムボム | **9こ以上**つなぐと出現。こわすと残り時間 **+5 びょう** |
| フィーバー | ゲージ満タンで 10 秒間スコア 2 倍・時間 +2 びょう |
| スキル | えらんだキャラのツムを消すとゲージがたまる。キャラごとに 6 種類 |
| 育成 | プレイでコイン入手 → 新キャラをおむかえ。使うほど Lv.5 までスキル強化 |
| 制限時間 | 60 びょう |

### キャラクターとスキル

| キャラ | スキル | 効果 | おむかえ |
| --- | --- | --- | --- |
| ぞうさん | にじのシャワー | よこ一列を消す | 最初から |
| うさちゃん | すいかバースト | まるく まとめて消す | 最初から |
| カピバラくん | ぷかぷかウェーブ | たて一列を消す | 最初から |
| キリンさん | ゆうやけクロス | じゅうじに消す | 600 コイン |
| パンダくん | ラムネクラッシュ | ランダムに消して +3 びょう | 1,000 コイン |
| ビーバーくん | かわづりダム | ツムを大ツム（3こぶん）に育てる | 1,500 コイン |

---

## ファイル構成

```
tsumtsum/
├─ engine/                  【Python】ゲームエンジン（ルールの本体）
│  ├─ zousan/
│  │  ├─ characters.py      キャラ・スキル・レベルの定義（ここが唯一の情報源）
│  │  ├─ physics.py         円の積み上げ物理（位置ベース／PBD）
│  │  ├─ board.py           盤面・チェーン判定・ボム・補充
│  │  ├─ rules.py           スコア／コイン／経験値の計算式
│  │  ├─ skills.py          6 種類のスキル効果
│  │  ├─ session.py         1 プレイぶんの状態（時間・コンボ・フィーバー）
│  │  └─ server.py          エンジンの HTTP API（標準ライブラリのみ）
│  ├─ export_config.py      キャラ定義を JSON に書き出す（PHP と共有）
│  ├─ simulate.py           ヘッドレス自動プレイ（バランス確認）
│  └─ tests/                エンジンのテスト 42 件
│
├─ src/                     【PHP】アプリの部品
│  ├─ App.php               設定の読み込み・プレイヤー・JSON 応答
│  ├─ Engine.php            Python エンジンへの接続
│  ├─ Store.php             SQLite（所持キャラ・コイン・経験値・ランキング）
│  ├─ Draw.php              GD で図形を描くための道具
│  └─ layout.php            画面の共通わく
│
├─ public/                  【PHP】Web ルート
│  ├─ index.php             タイトル
│  ├─ play.php              ゲーム画面
│  ├─ characters.php        キャラの選択とおむかえ
│  ├─ ranking.php / help.php
│  ├─ api/                  session / action / finish / player
│  └─ assets/
│     ├─ js/client.js       描画と入力だけの薄い層（サーバー版と静的版で共通）
│     ├─ js/engine-local.js 静的版のブラウザ内エンジン（Python の移植）
│     ├─ js/shell.js        静的版の画面まわり（PHP のページの代わり）
│     ├─ js/audio.js        効果音と BGM（WebAudio で合成／音源ファイル無し）
│     ├─ css/app.css
│     ├─ img/               PHP-GD が描いたキャラ画像とアプリアイコン
│     └─ config.json        Python が書き出したマスタデータ
│
├─ tools/
│  ├─ render_assets.php     【PHP】キャラ画像とアプリアイコンを描く
│  └─ build_static.php      【PHP】Netlify 用の dist/ を書き出す
├─ dist/                    Netlify に置くフォルダ（make static で作りなおす）
├─ Dockerfile               サーバー版をコンテナで動かす
├─ docker-compose.yml       docker compose up で起動
├─ bin/serve                エンジンと Web アプリをまとめて起動
└─ Makefile

（リポジトリ直下の `netlify.toml` は Netlify、`render.yaml` は Render に置くときの設定です）
```

---

## 中身のはなし

### 物理（`engine/zousan/physics.py`）

速度をそのまま積分すると、支えられた円に重力が溜まりつづけて山が永久に振動します。
そこで **位置ベース（PBD）** にしています。

1. 重力を加えて仮の位置へ動かす
2. めり込みを「位置」で 5 回くり返し解く
3. 実際に動いた距離から速度を作り直す

支えられた円は位置が動かない＝速度が自動的に 0 になるので、山がぴたりと止まります。
さらに次の 3 つを入れて、1 手あたり 39ms に収めました。

- **ねむり**: 動きが止まった円を計算から外す（盤面のほとんどは動かない）
- **ペアの使い回し**: ぶつかりそうな組み合わせを 1 ステップに 1 回だけ作る
- **摩擦**: 接触面の横ずれを打ち消す。これが無いと円が転がりつづけて何秒も静止しない

打ち切りの判定は「下向きの速さ」だけを見ています。ゆるい山は横方向にいつまでも
小さくゆれるので、速さ全体で判定すると毎回上限まで回ってしまい、
まだ落ちている途中のツムをそのまま固めてしまう（宙に浮いたまま止まる）ためです。
さらに仕上げとして、どこにも触れていない円が残っていたら着地するまで回すので、
浮いたツムは絶対に残りません（`World.floating_bodies()` とテストで担保）。

### 音（`public/assets/js/audio.js`）

音源ファイルを持たず、WebAudio でその場で合成しています。
つなぐたびに音階が上がり、フィーバー中は BGM が速くなります。
プレイ画面右上の ♪ ボタンでオン／オフでき、設定は localStorage に残ります。

### キャラクターの絵（`tools/render_assets.php`）

画像ファイルを持たず、GD の図形だけで描いています。
4 倍の大きさで描いてから縮めることで、輪郭のギザギザを消しています。
色や種族は `characters.py` の定義を読むので、**キャラを増やすときは Python 側を直すだけ**です。

```python
# engine/zousan/characters.py に 1 件足す
Character(
    id="kuma", name="くまさん", tagline="もりの ともだち",
    kind="beaver",              # 描きかたの種類（render_assets.php の switch）
    body="#C08A5A", shade="#7A5230", inner="#EBD3B6",
    cheek="#FF9C9C", accent="#E8C98A", price=2000,
    skill=Skill(id="burst", name="はちみつバースト", desc="まるく 消す",
                need=[24, 22, 20, 18, 16], power=[110, 126, 142, 160, 180]),
)
```

```bash
make assets   # 定義の書き出し → 画像の生成。選択画面にもゲームにも自動で出てくる
```

新しい絵の描きかたを増やしたいときは `tools/render_assets.php` の `drawByKind()` に
`case` をひとつ足します。新しいスキル効果は `engine/zousan/skills.py` の `TABLE` に足します。

### 静的版のエンジン（`public/assets/js/engine-local.js`）

オフラインで遊ぶには、ルールがブラウザの中に無いといけません。
そこで Python エンジンと**同じ手順**で動く JavaScript 版を置いています。
数値はどちらも `config.json`（もとは `engine/zousan/` の Python）を読むので、
二か所で数字を持つことはありません。

描画側（`client.js`）は、サーバーに投げるかわりにこのエンジンへ問い合わせるだけなので、
**サーバー版と静的版で同じコード**が動いています。

```js
// client.js の中身（抜粋）
if (window.LocalEngine && window.LocalEngine.enabled) {
  return LocalEngine.request(url, body);   // 静的版：ブラウザの中で計算
}
return fetch(url, ...);                    // サーバー版：PHP → Python
```

### データ

`data/zousan.sqlite` が自動で作られます（プレイヤー・所持キャラ・経験値・ランキング）。
プレイヤーの見分けは Cookie のセッションだけで、ログインは要りません。

---

## 本番に置くとき

- Web ルートを `public/` に向けてください（`src/` と `engine/` は公開されません）。
- Python エンジンは `127.0.0.1` で待ち受けます。外に出さないでください。
  接続先は環境変数 `ENGINE_URL` で変えられます。
- エンジンは systemd などで常駐させ、PHP は Apache / nginx + PHP-FPM から動かすのが手軽です。
